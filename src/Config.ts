const INFURA_ID = "";

export default {

    chainId: 42,
    infuraId: INFURA_ID,

    // Kovan
    endpoint: `wss://kovan.infura.io/ws/v3/${INFURA_ID}`,

    // Mainnet
    //endpoint: `wss://mainnet.infura.io/ws/v3/${INFURA_ID}`,

    contracts: {
    },
};
